replicaCount: 2

image:
  repository: demo-app
  tag: v1
  pullPolicy: Never

appConfig:
  APP_ENV: stage
  LOG_LEVEL: info

service:
  type: NodePort
  port: 80
  targetPort: 80

resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "500m"
    memory: "256Mi"
